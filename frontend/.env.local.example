# Copy this file to `.env.local` (gitignored) and adjust as needed.
# Vite injects any var prefixed with VITE_ into `import.meta.env`.

VITE_API_URL=http://localhost:8000
VITE_DEMO_TRIP_ID=trip_lisbon
